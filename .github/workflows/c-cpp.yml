name: C/C++ CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - name: Build
      shell: bash
      run: cd ..
    
    - name: Build
      shell: bash
      run: cd tools
